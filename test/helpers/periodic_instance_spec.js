var expect   = require('chai').expect,
    path     = require('path'),
    instance = require('./periodic_instance');

describe('The periodic Stub', function(){
  it('should know the path for previous dir', function(){

  });
  it('should create a folder in the previous dir to install the instance', function(){

  });
  it('should install periodic in the dir', function(){

  });
  it('should start the server on the periodic folder', function(){

  });
  it('should stop the serve once the test have completed', function(){

  });
});

